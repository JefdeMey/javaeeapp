server.port=${PORT:8080}
info.app.name=spring
info.app.description=This is my first spring boot application
info.app.version=1.0.0

vaadin.frontend.hotdeploy=true
vaadin.productionMode=false

logging.level.root=error
logging.level.be.ucll=info

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.open-in-view=false

# SQL-initialisatie via plain JDBC (voor JPA-initialisatie, geen JTA-interferentie)
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# ActiveMQ — embedded in-VM broker (geen externe installatie nodig)
spring.activemq.broker-url=tcp://localhost:61616
spring.activemq.user=admin
spring.activemq.password=admin

# Java Mail — Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=jefdemey@gmail.com
spring.mail.password=piiv njiz wxas ujbd
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
mail.afzender=jefdemey@gmail.com
